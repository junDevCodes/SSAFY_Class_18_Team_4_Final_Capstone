# 🚀 SelF

> 소비자가 원하는 상품을 추천하고, 가장 저렴하게 찾을 수 있는 전자상거래 플랫폼

## 1. 프로젝트 개요

### 1.1 개요

- **프로젝트명**: SelF
- **프로젝트 유형**: 웹 서비스 플랫폼
- **상태**: 개발 중
- **타임라인**: 2025년 11월 10일 – 진행 중
- **팀 규모**: 3인

### 1.2 문제 설명

실시간으로 변동되는 물품 가격에 대응하며 사용자의 취향을 반영하는 추천 서비스가 부족하고, 복잡한 중간 유통 과정으로 인한 가격 거품이 발생하고 있다.  
소비자는 “무엇을 사야 할지, 언제 사야 가장 합리적인지”를 동시에 판단하기 어렵고, 판매자는 소비자의 취향을 세밀하게 반영해 상품을 노출하기 어렵다.

### 1.3 솔루션

SelF는 **크롤링 기반 실시간 가격·상품 데이터**를 중심으로, **Instacart 공개 데이터**를 보조적으로 활용하여 아래를 제공하는 전자상거래 플랫폼이다.

- 실시간 가격 변동성과 사용자의 취향을 동시에 반영하는 **개인화 추천 서비스**
- 생산자와 소비자를 직접 연결하여 중간 유통 단계를 최소화하는 **직거래 지향 구조**
- Instacart 데이터로 학습한 패턴을 활용해 **콜드스타트 사용자를 보완하는 보조 추천(AIRScout)** 기능

### 1.4 핵심 가치

- **개인화**: 사용자의 장바구니 이력, 클릭/조회 기록, 선호 카테고리를 반영한 초개인화 추천
- **가격 효율성**: 시계열 기반 분석으로 최저가 구간을 포착하고, 사용자가 평소 보던 상품을 “지금 사야 하는 이유”와 함께 제시
- **신선도/신뢰성**: 생산자–소비자 직거래를 통해 유통 단계를 줄이고, 신선한 식재료를 합리적인 가격에 제공

### 1.5 핵심 기능 개요

- **AIRScout (보조 추천 알고리즘 고도화)**  
  사용자 데이터가 희박한 상황에서도 Instacart 기반 패턴과 크롤링된 상품 임베딩을 활용해 취향을 반영하는 보조 추천 알고리즘.
- **시계열 기반 최저가 추천**  
  크롤링된 가격 시계열과 Prophet 기반 예측을 이용해, 현재 가격이 “예상보다 얼마나 싸졌는지”를 점수화하여 최저가 상품을 추천.
- **레시피 기반 Gap Filling**  
  장바구니에 담긴 재료와 레시피 재료를 비교해 부족한 재료를 추천하고, 자연스러운 장바구니 완성을 돕는 레시피 기반 추천.

### 1.6 대상 고객

- **주요 사용자**  
  자취생, 주부, 합리적인 가격에 신선한 식재료를 원하는 모든 개인 사용자.  
  자신의 취향에 맞는 상품 추천과 가격 변동에 따른 최저가 알림, 판매자와의 직접 거래를 통해 **신선한 식품을 합리적인 가격으로 구매**하고자 하는 사용자.

- **보조 사용자**  
  대규모로 좋은 재료가 필요한 기업/식당, 또는 단체 구매가 필요한 개인.  
  이들을 위해 **별도의 거래의 장**을 마련하여 대량 구매/계약 기반 거래도 지원할 수 있는 구조를 지향한다.

## 2. 기술 스택 (Tech Stack)

| 구분                    | 기술                                                                              | 설명 및 선택 이유                                                                                                                                                                                                                                                           |
| ----------------------- | --------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Frontend**            | **Vue 3, TypeScript**                                                             | 컴포지션 API와 타입 안전성을 활용해 복잡한 상태(장바구니, 추천 리스트, 가격 그래프)를 안정적으로 관리. 반응형 UI로 가격·추천 결과를 실시간에 가깝게 갱신하며, 팀 생산성을 위해 학습曲선이 완만한 Vue를 선택.                                                                |
| **Backend**             | **Django, Django REST Framework (DRF)**                                           | 사용자·상품·주문·거래 구조를 안정적으로 모델링하기 위한 ORM과 인증/권한 기능 제공. 관리 페이지, 세션/토큰 인증 등 전자상거래 기본 기능을 빠르게 구축하기 위해 선택.                                                                                                         |
| **ML API**              | **FastAPI**                                                                       | Bert4Vec 임베딩, 시계열 예측, 추천 결과 계산 등 ML/DL 연산을 별도의 경량 서비스로 분리하기 위해 사용. 비동기 지원과 높은 성능, 명확한 스키마 정의(Pydantic)로 실시간 추천 API 구현에 적합.                                                                                  |
| **Crawling / Scraping** | **Playwright**                                                                    | 실제 이커머스/상품 페이지를 대상으로 한 동적 렌더링 환경에서의 크롤링을 안정적으로 수행. 브라우저 자동화를 통해 가격·상품 정보·메타데이터를 주기적으로 수집하여 실시간성 있는 데이터 베이스를 유지.                                                                         |
| **ML / DL**             | **Pandas, NumPy, Scikit-Learn, Keras, Prophet, Sentence-Transformers (Bert4Vec)** | Pandas/NumPy로 데이터 전처리, Scikit-Learn으로 기본 피처 엔지니어링과 서포트 모델 구성. Keras로 필요 시 심층 모델 구성, Prophet으로 가격 시계열 예측, Sentence-Transformers(Bert4Vec)로 상품/레시피/사용자 임베딩 생성 및 추천에 활용.                                      |
| **Database / Cache**    | **PostgreSQL, Redis**                                                             | PostgreSQL로 사용자·상품·주문·레시피·가격 이력 등 관계형 데이터 관리. Redis는 세션/토큰, 인기 상품/추천 결과 캐싱, 큐 기반 작업(비동기 계산 중간 결과) 저장에 활용.                                                                                                         |
| **Storage**             | **AWS S3, Nginx (정적 리소스)**                                                   | 상품 이미지 및 정적 파일을 S3에 저장하고, Presigned URL을 통해 안전하게 접근. Nginx를 통해 정적 리소스 서빙과 리버스 프록시 역할을 수행.                                                                                                                                    |
| **CI/CD & Infra**       | **Docker, Docker Compose, GitHub Actions, AWS EC2, AWS ECR, Nginx**               | 애플리케이션을 컨테이너 단위로 관리하여 환경 의존성을 최소화하고, Docker Compose로 로컬/스테이징/프로덕션 환경을 통일. GitHub Actions로 테스트·빌드·배포 파이프라인을 자동화하고, AWS ECR에 이미지 저장 후 EC2에 자동 배포. Nginx는 리버스 프록시 및 정적 파일 서빙에 사용. |

## 3. 데이터 아키텍처 (Data Architecture)

### 3.1. Core Data (Instacart Dataset)

_기존 Kaggle 데이터셋 활용_

- **Users**: `orders.csv`의 `user_id`.
- **Products**: `products.csv` (ID, Name, Aisle, Department).
- **Orders**: `orders.csv` (Order ID, User ID, Order DOW, Hour, Days Since Prior).
- **Order Products**: `order_products__*.csv` (Order ID, Product ID, Add to Cart Order, Reordered).
- **Categories**: `aisles.csv`, `departments.csv`.

### 3.2. Extended Data (Generated/Mock)

_서비스 구현을 위해 추가 생성 필요한 데이터_

- **Product Prices**: Instacart 데이터에 가격이 없으므로 생성 필요.
  - 구조: `ProductPrice(product_id, price, effective_date)`.
  - 생성 로직: 카테고리별 평균가 설정 후 정규분포 기반 랜덤 생성 + 시계열 변동 부여. **이유**: EDA에서 시간 패턴(요일별 재주문)이 있으므로, 가격 시계열 데이터로 최저가 추천을 시뮬레이션.
- **Recipes**: Gap Mapping을 위한 레시피 데이터.
  - 구조: `Recipe(name, ingredients_list, complexity, category)`.
  - 소스: 외부 API(Spoonacular) 참조 또는 더미 데이터 50~100개 구축. **이유**: EDA에서 장바구니 패턴(add_to_cart_order 기반 재주문 65%)이 강하므로, 레시피를 활용해 gap을 매핑하면 연관성 높은 추천 가능.

## 4. 핵심 기능 및 알고리즘 (Core Features & Algorithms)

### 4.1 데이터 및 EDA 기반 인사이트

SelF는 **실서비스에서 크롤링한 상품·가격 데이터**를 주 데이터 소스로 사용하고, **Instacart 공개 데이터셋**은 콜드스타트 보완 및 패턴 사전 학습(pre-training) 용도로 활용한다.

- Instacart 데이터 분석 결과, **재주문 비율이 높고(long-tail 구조)**, 특정 상품과 카테고리에 대한 편향이 강하게 나타난다.
- 사용자별 주문 시퀀스를 분석하면, 주문 간격·요일·시간대에 따른 **명확한 시계열 패턴**이 존재한다.
- SelF의 크롤링 데이터에서는, 실제 마켓 가격의 **단기·중기 변동성**과 함께, 특정 이벤트/프로모션 시기의 급격한 가격 변동이 관찰된다.

이러한 인사이트를 바탕으로, SelF는 **텍스트 임베딩(Bert4Vec) + 시퀀스 기반 임베딩(Prod2Vec 계열) + 시계열 예측(Prophet)**을 결합한 하이브리드 구조를 채택한다.

### 4.2 Feature 1: AIRScout – 개인화/보조 추천 알고리즘

> “사용자 데이터가 적어도, 취향을 최대한 반영한 상품을 추천합니다.”

**목표**  
사용자 데이터가 충분한 경우에는 강한 개인화를, 데이터가 희박한 경우(cold/sparse user)에는 Instacart와 전역 패턴을 활용해 **취향을 추정하는 보조 추천(AIRScout)**을 제공한다.

**알고리즘 구성 (Bert4Vec + Prod2Vec 하이브리드)**

1. **상품 텍스트 임베딩 (Bert4Vec / Sentence-Transformers)**

   - Input: 상품명, 카테고리, 설명, 원산지/브랜드 등 텍스트 메타데이터.
   - Process: Sentence-Transformers 기반 모델(Bert4Vec)로 각 상품을 고차원 임베딩 벡터로 변환.
   - Output: `item_text_vector`.

2. **시퀀스/코구매 임베딩 (Prod2Vec)**

   - Input: SelF 장바구니/구매 시퀀스 + Instacart 주문 시퀀스(`order_products__prior`).
   - Process: Word2Vec 계열 모델로 “상품 시퀀스”를 학습하여, 자주 함께 구매되는 상품이 가까운 공간에 위치하도록 임베딩.
   - Output: `item_seq_vector`.

3. **사용자 임베딩 구성**

   - Warm 유저:
     - 최근/자주 구매한 상품들의 `item_text_vector`, `item_seq_vector`를 가중 평균하여 `user_text_vector`, `user_seq_vector`를 생성.
   - Cold/Sparse 유저:
     - Instacart 기반 전역 인기/코구매 패턴 + 최소한의 클릭/조회 이력을 활용해 초기 `user_seq_vector`를 구성.
     - 프로필/선호 카테고리가 있다면 Bert4Vec로 텍스트 임베딩 후 `user_text_vector`에 반영.

4. **추천 점수 계산**

   - 각 후보 상품 \(i\)에 대해 다음과 같은 하이브리드 점수를 계산:
     - \( s*i = \alpha \cdot \cos(user_text, item_text_i) + \beta \cdot \cos(user_seq, item_seq_i) + \gamma \cdot f*{\text{price/popularity}}(i) \)
   - \(\alpha, \beta, \gamma\)는 서비스 정책에 따라 조정하며, 가격 민감도/신선도/인기도 등을 반영하는 피처를 함께 사용한다.

5. **AIRScout로서의 역할**
   - 데이터가 충분한 사용자는 개인화 점수를 중심으로 추천.
   - 신규 사용자/희박 사용자에게는 Instacart에서 사전 학습된 코구매 패턴과 텍스트 유사도 기반 추천을 제공하여 **콜드스타트를 최소화**한다.

### 4.3 Feature 2: 가격 변동에 따른 최저가 추천 (시계열 분석)

> “평소에 보던 그 상품, 지금이 ‘예상보다 얼마나’ 싸졌는지 알려줍니다.”

**목표**  
크롤링으로 수집한 상품별 가격 시계열을 분석하여, **현재 가격이 예측된 정상 구간 대비 얼마나 저렴한지**를 정량화하고, 사용자의 관심 상품 중 “지금 사기 좋은” 후보를 추천한다.

**알고리즘 구성 (Prophet 기반 시계열 예측 + Residual 분석)**

1. **데이터 수집 및 전처리**

   - 각 상품별로 크롤링을 통해 일/시간 단위 가격 이력을 수집.
   - 이상치/누락 값 처리, 프로모션/이벤트 플래그 등 메타정보를 함께 저장.

2. **상품별 Prophet 모델 학습**

   - 일정 수준 이상의 데이터가 누적된 상품에 대해 Facebook Prophet 모델을 학습:
     - \( y_t = \text{trend}\_t + \text{seasonality}\_t + \text{holiday}\_t + \epsilon_t \)
   - 주간/월간 시즌성, 프로모션 기간 등으로 인한 패턴을 분리하여 “정상 가격 궤적”을 추정.

3. **예측 대비 할인도(Residual) 계산**

   - 예측값 \(\hat{P_t}\)와 실제 가격 \(P_t\)에 대해:
     - \( \text{residual}\_t = \hat{P_t} - P_t \)
     - \( \text{discount*score}\_t = \frac{\text{residual}\_t}{\sigma*{\text{residual}}} \)
   - `discount_score`가 클수록 “예상보다 많이 할인된 상태”로 해석.

4. **최저가 후보 필터링**

   - 조건 예시:
     - `discount_score_t > θ` (예: 1.5 이상)
     - 최근 N일 중 최저 가격 대비 일정 비율 이상 추가 할인
   - 위 조건을 만족하는 상품 중,
     - 사용자의 관심 상품(조회/장바구니/구매 이력 존재)
     - AIRScout가 추천한 후보 상품  
       에 대해 최저가 알림/추천을 노출.

5. **Fallback 전략**
   - 데이터가 충분치 않아 Prophet을 적용하기 어려운 상품은, 기존 이동평균·표준편차 기반 threshold 방식(30일 이동평균 – \(k\sigma\))을 사용해 간단히 최저가 후보를 추출한다.

### 4.4 Feature 3: 레시피 기반 Gap Filling (Recipe-based Recommendation)

> “카레 가루와 양파를 담으셨네요? 감자와 당근은 어떠세요?”

**목표**  
장바구니에 담긴 재료를 기반으로, 사용자가 만들 수 있을 법한 레시피를 탐색하고, **레시피 재료 집합과 현재 장바구니의 차이(Gap)를 채워주는 추천**을 제공한다.

**알고리즘 구성 (Bert4Vec 기반 레시피 검색 + Gap 분석)**

1. **레시피 임베딩**

   - Input: 레시피 이름, 설명, 재료 리스트(각 재료의 상품명/카테고리 포함).
   - Process: Sentence-Transformers(Bert4Vec)로 하나의 문장/문서로 합친 뒤 임베딩 → `recipe_vector`.
   - Output: 각 레시피당 1개 벡터.

2. **장바구니 임베딩**

   - 현재 장바구니에 담긴 상품들의 Bert4Vec 임베딩을 평균/가중합하여 `cart_vector`를 생성.
   - 필요 시, Prod2Vec 기반 시퀀스 임베딩을 함께 사용하여 “함께 자주 쓰이는 재료 조합” 정보를 반영.

3. **유사 레시피 후보 검색**

   - `cosine(cart_vector, recipe_vector)`를 기준으로 상위 K개의 레시피를 후보로 선정.
   - 특정 카테고리(한식/양식/디저트 등)나 난이도, 조리 시간 등 추가 필터를 적용 가능.

4. **Gap 분석 및 추천 재료 도출**

   - 각 후보 레시피 \(R\)에 대해, 레시피 재료 집합을 \(R\), 현재 장바구니 재료 집합을 \(C\)라고 할 때:
     - 유사도 점수 \( S = \frac{|C \cap R|}{|R|} \) (Jaccard 변형)
   - \(S > S\_{\text{th}}\) (예: 0.5) 인 레시피에 대해 **부족한 재료 집합 \(R - C\)** 를 Gap으로 정의하고, 추천 리스트로 노출.

5. **우선순위 정렬**
   - Instacart 및 SelF 실제 로그 데이터를 이용해, 각 Gap 재료에 대해 아래 요소를 반영해 점수화:
     - 다른 사용자들이 해당 레시피를 완성할 때 자주 함께 구매한 빈도(코구매율)
     - 현재 가격(Feature 2의 discount_score)
     - 사용자의 선호 카테고리/브랜드
   - 최종적으로 Gap 재료를 점수 순으로 정렬하여 “이 레시피를 완성하려면, 이 재료들을 추가로 담아보세요” 형태로 UI에 노출한다.

## 5. 시스템 아키텍처 (System Architecture)

### 5.1 전체 구조 개요

- **사용자 요청 라인**
  - 브라우저(Vue3) → **Nginx(리버스 프록시)** → **Django + DRF(메인 API)** → **FastAPI(추천·데이터 API)** → **PostgreSQL / Redis / S3**.
- **데이터·배치 라인**
  - 외부 마켓 → **Crawler 배치 서비스** → **S3(raw)** + **PostgreSQL(정제 데이터)** → **ML/피처 파이프라인** → **S3(models, features)** → **FastAPI**(온라인 서빙).
- **배포 라인**
  - Git → **GitHub Actions(CI/CD)** → **Docker 이미지 빌드** → **AWS ECR** → **AWS EC2**에 자동 배포, Nginx를 통해 각 서비스 라우팅.

### 5.2 Backend & API 레이어

- **Django + DRF (메인 API)**

  - 역할: 인증/인가, 유저·프로필·장바구니·주문·레시피 등 비즈니스 도메인 처리, 프론트엔드의 단일 진입점.
  - 데이터 접근: PostgreSQL read/write, Redis 세션/캐시 사용.
  - 추천/최저가/Gap 결과가 필요할 때 FastAPI에 내부 HTTP 요청을 보내고, 응답을 가공해 프론트에 전달.

- **FastAPI (추천·데이터 API)**
  - 역할: Bert4Vec/Prod2Vec/Prophet/Gap Filling 기반 추천·분석 로직을 서빙하는 전용 서비스.
  - 데이터 접근: PostgreSQL read-only, Redis/S3에서 피처·모델·캐시 조회.
  - Django에서 전달받은 `user_id`, 장바구니, 컨텍스트 정보를 입력으로 받아 점수/랭킹/최저가 여부 등을 계산해 반환.

### 5.3 데이터 수집 & ML 배치 레이어

- **Crawler 서비스**

  - 외부 마켓(예: 이커머스 사이트)에서 상품·가격·이미지·메타데이터를 주기적으로 수집.
  - 원본(raw) 데이터는 S3에 저장하고, 정제된 상품·가격 이력은 PostgreSQL의 `products`, `product_offers`, `price_history` 등 테이블에 적재.

- **ML/피처 파이프라인**
  - Instacart + 크롤링 데이터를 활용해 사용자/상품 피처, 코구매 통계, 가격 패턴 등을 계산하고, Bert4Vec/Prod2Vec/Prophet 모델을 학습.
  - 학습된 모델과 피처 스냅샷은 S3(`models/`, `features/`)에 저장되며, FastAPI 인스턴스에서 주기적으로 로드해 온라인 추천에 사용.

### 5.4 스토리지 & 인프라 레이어

- **PostgreSQL**: 사용자, 상품, 주문, 가격 이력, 레시피, 이벤트 로그 등 서비스 핵심 데이터를 저장하는 운영 DB.
- **Redis**: 세션/토큰 관리, 추천 결과·인기 상품·일부 피처 캐시 등 고속 조회가 필요한 데이터 관리.
- **S3**: 크롤링 raw 데이터, 상품 이미지, 학습된 모델 파일, 피처 덤프 등 대용량/파일성 데이터 저장소.
- **Nginx + Docker + AWS EC2 + AWS ECR + GitHub Actions**:
  - Nginx는 정적 리소스(Vue 빌드 결과) 서빙과 Django/FastAPI로의 리버스 프록시 역할을 수행.
  - 모든 서비스는 Docker 컨테이너로 배포되며, GitHub Actions를 통해 테스트·빌드·ECR 푸시·EC2 배포 파이프라인을 자동화.

### 5.5 Frontend 레이어

- **Vue 3 + TypeScript 프론트엔드**
  - 위치: `frontend/`
  - 역할: 홈(추천 대시보드), 상품 상세(가격 시계열/최저가 알림), 장바구니(Gap Filling 결과) 등 사용자 인터페이스를 담당.
  - Django 메인 API와 통신하며, 추천/최저가/Gap 결과를 실시간에 가깝게 시각화한다.

## 6. 코구매율 정의

- **코구매율(Co-purchase Rate)**
  - **정의**: 특정 상품 A를 포함하는 주문들 중에서, 상품 B도 함께 포함된 주문의 비율.
  - **수식**: \(\text{코구매율}(A \rightarrow B) = \dfrac{\text{A와 B를 모두 포함하는 주문 수}}{\text{A를 포함하는 주문 수}}\).
  - **예시**: A를 산 주문이 40건이고, 그 중 A와 B를 같이 산 주문이 10건이면, \(\text{코구매율}(A \rightarrow B) = 10 / 40 = 0.25\)로, “A를 산 사람의 25%가 B도 같이 샀다”로 해석할 수 있다.
  - **활용**: SelF에서는 레시피 Gap Filling과 추천 우선순위 결정 시, 어떤 재료/상품 조합이 실제 사용자 장바구니에서 **얼마나 자주 함께 등장하는지**를 나타내는 지표로 사용한다.

## 7. 한계점 및 개선 방향

- **버전 및 호환성 관리**

  - Django–FastAPI 간 요청/응답 스키마를 Pydantic 기반 공용 모델로 정의하고, `/data-api/v1/...`와 같은 버전 접두어를 사용해 API 진화를 관리한다.
  - 필드 변경 시에는 추가는 optional로, 의미 변경/삭제는 새 필드로 대체하는 방식으로 브레이킹 체인지를 최소화한다.

- **단일 DB(PostgreSQL)에 대한 부하 집중**

  - 자주 사용하는 컬럼에 적절한 인덱스를 부여하고, 홈 추천·인기 상품·특가 목록 등은 Redis TTL 캐시를 통해 조회 부하를 줄인다.
  - 추천/통계용 쿼리는 배치에서 미리 전처리한 스냅샷·집계 테이블을 활용하도록 설계해, 운영 트랜잭션 쿼리와의 충돌을 완화한다.

- **크롤링 기반 데이터 수집의 취약성**

  - 크롤러는 애플리케이션과 분리된 배치 서비스로 두어, 실패하더라도 최근 스냅샷 데이터를 사용해 서비스는 지속 가능하도록 한다.
  - 크롤링 성공/실패, 수집 건수, 실행 시간 등을 모니터링하고, 구조 변경·차단·속도 제한 징후가 보이면 알림을 통해 빠르게 대응한다.

- **모델/피처 버전 관리 부족**

  - `ml_model_registry`와 같은 모델 레지스트리 테이블을 두어 `model_name`, `version`, `s3_path`, `is_active` 등을 관리하고, FastAPI는 활성화된 버전만 로드한다.
  - S3에서는 `models/{model_name}/{version}/...` 형태로 디렉터리 구조를 고정하고, 필요 시 피처 스키마를 JSON으로 함께 관리해 모델 교체 시 혼선을 줄인다.

- **로그/모니터링 미비**

  - Django와 FastAPI 모두에서 `trace_id`, `user_id`, `endpoint`, `status_code`, `latency_ms`, `model_version` 등을 포함한 구조적(JSON) 로그를 남긴다.
  - `/health` 엔드포인트 및 기본 메트릭(요청 수, 에러율, P95 레이턴시 등)을 먼저 구축하고, 임계치를 넘을 경우 Slack/메일 알림으로 이상 징후를 인지한다.

- **FastAPI 도입 초기 단계의 복잡도**
  - FastAPI는 `/health`, `/recommend/home` 등 핵심 엔드포인트부터 최소 기능으로 도입하고, 점진적으로 Bert4Vec/Prod2Vec/Prophet/Gap Filling 기능을 분리·이관한다.
  - 각 기능 이관 시 모델 레지스트리와 로그에 모델 버전을 명시해, 추천 품질과 장애를 버전 단위로 추적할 수 있도록 한다.
